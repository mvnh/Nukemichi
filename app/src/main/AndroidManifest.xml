<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Predictive-back's partial-shrink gesture preview conflicts with Navigation3's
         NavDisplay transition (visible glitch on pop) until nav3 ships proper support. -->
    <application
        android:name=".NukemichiApp"
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.App"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.App">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".core.vpn.internal.NukemichiVpnService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse"
            android:process=":vpn">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-initiated private network tunnel" />
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>
        <!-- Same :vpn process as NukemichiVpnService (shares its Hilt singletons, incl.
             XrayTelemetryMonitor) — bindable by our own app for the Messenger telemetry
             channel, unlike NukemichiVpnService itself whose BIND_VPN_SERVICE permission is
             system-only. See VpnIpcService/RemoteXrayMonitoring. -->
        <service
            android:name=".core.vpn.internal.VpnIpcService"
            android:exported="false"
            android:process=":vpn" />
    </application>

</manifest>
